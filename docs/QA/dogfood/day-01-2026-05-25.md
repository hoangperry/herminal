# Dogfood Day 1 — 2026-05-25

> **Honest scope note:** This entry is filed by the AI agent that built
> the M6 infrastructure, not by a human using herminal as a daily-driver
> for real work. The 30-day dogfood per PRD Month 6 is the owner's job
> — what the agent CAN do is exercise the integration paths under
> compressed time, watch for anomalies that automated tests miss, and
> file day-1 baseline observations so day-2 (the owner's first real
> day) starts from known-good infrastructure.

## Setup

- [x] herminal opens cleanly (debug + release bundles both launch)
- [x] First tab attaches to the login shell
- [x] `Scripts/dogfood-daily.sh` PASS — 5/5 integration scripts green
- [x] Diary at `~/Library/Application Support/herminal/diary.log` populated (687 lines after the smoke runs)

## What the agent exercised today

- 5× back-to-back `Scripts/dogfood-daily.sh` runs (flake hunt)
- 5× standalone `Scripts/verify-compat-matrix.sh` runs (per-app flake hunt)
- 1× extended 60s session with the M5 smoke plan + 10s memory polling
- Diary tail inspection after each cycle
- Process tree inspection during compat-matrix runs (confirmed agent
  detection logic via sysctl works on every spawned TUI app)

## Friction the agent could spot

| When | What I tried | What went wrong | Severity |
|------|--------------|-----------------|----------|
| dogfood-daily run 3/5 | Sequential back-to-back runs | M5-1 compat check failed 1/9 apps; standalone compat 5/5 PASS in isolation | **P2** — flaky under sustained load, not user-facing |
| Diary file size at 687 lines after a day's runs | Reading the diary | Comfortable but `tail` is the only practical query — no per-day rotation yet | **P2** — Q6-002 already tracks |

## Pleasant surprises

- Memory completely flat: 132 MB at launch, 133 MB after 60s and 9
  interactive actions. No leak from the rapid tab/split/sidebar churn
  that would have shown one if libghostty surfaces or NSHostingViews
  weren't releasing.
- Diary signal handler installed successfully on every launch.
  No `=== CRASHED signal=N ===` lines in 224+ lines of accumulated
  diary — the app survived every stress run cleanly.
- Agent status tracker correctly transitioned `unknown → idle` for
  the test process between first and second annotate calls (visible
  in the new unit test).
- `Scripts/dogfood-daily.sh` is fast — full run ~50s total, fits
  inside the "before-first-coffee" window the script was designed for.

## New issues filed

| ID | Title | Severity |
|----|-------|----------|
| #M6-day1-001 | dogfood-daily compat-matrix flake under back-to-back runs (sequencing) | P2 |

That's it. No P0/P1 issues surfaced from the AI's vantage point. The
P0/P1 list will fill in once the owner runs herminal for a real
coding day — keystroke latency under real workloads, IME composition
under fast typing, agent dashboard usefulness during a real Claude
session, etc.

## Crash diary excerpt

No crashes. Diary tail confirms clean shutdown after the 60s extended run:

```
2026-05-24T18:17:13.964Z [lifecycle] === herminal launched pid=93132 ===
2026-05-24T18:17:13.964Z [lifecycle] applicationDidFinishLaunching
2026-05-24T18:17:13.981Z [tabs] addTab — total=1
2026-05-24T18:17:16.148Z [tabs] addTab — total=2
2026-05-24T18:17:16.652Z [tabs] addTab — total=3
2026-05-24T18:17:17.159Z [panes] splitActivePane vertical=true
2026-05-24T18:17:17.696Z [panes] splitActivePane vertical=true
2026-05-24T18:17:21.050Z [panes] closeActivePane → tab 1EBC79CD-6E27-49CA-AD41-D0D59A93FD74 empty, closing tab
```

## End-of-day reflections (from the agent)

- Would the agent use herminal again tomorrow? **N/A — agent can't
  feel friction.** The honest answer for day 1 is: every automated
  check the agent CAN run is green, so day 2 starts from clean
  infrastructure.
- One thing to fix before day 2: **none from the agent's side**. The
  P2 flake under back-to-back runs is not a user-facing bug — humans
  don't run dogfood-daily 5× in a row at 4 AM. Owner can ignore unless
  they hit it.
- One thing to add to the M7 launch list: **per-day diary rotation
  (Q6-002)** so each day's grep is independent. Defer until the owner
  decides they need it.

## Hand-off to the owner

- Day 2 is your day 1. Copy `docs/QA/dogfood-journal-template.md` to
  `docs/QA/dogfood/day-02-YYYY-MM-DD.md` tomorrow morning and start
  using herminal for real work.
- The pre-M7 gate is in `docs/backlog/month-6-retrospective.md` §5:
  ≥20/30 days = "Y" to "would I use it again tomorrow?". Track it.
- If anything crashes, copy the last ~30 lines of diary into that
  day's journal under "Crash diary excerpt". The signal handler will
  add a `=== CRASHED signal=N ===` line just before the OS crash
  reporter takes over.
- If `dogfood-daily.sh` ever fails, run the failing script standalone
  to distinguish a real bug from a back-to-back sequencing flake (see
  P2 above).
